import { Posts } from "@/components/posts";

export default async function DesingTools() {
  return (
    <main className="min-h-screen">
      <div className="flex min-h-screen flex-col mx-auto p-10">
        <Posts />
      </div>
    </main>
  );
}
